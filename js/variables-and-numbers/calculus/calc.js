const circleRadius = 5;
const circleArea = Math.PI * Math.pow(2.5, 2);
const circleCircumference = 2 * Math.PI * 2.5;

console.log(
  "For a circle with a radius of 5 units: The area is approximately",
  circleArea,
  "The circumference is approximately",
  circleCircumference,
  "units"
);
